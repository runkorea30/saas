/**
 * 모바일 수동주문입력 — 다음 단계 구현 예정.
 */
import { StubPage } from './StubPage';

export function OrderInputPage() {
  return (
    <StubPage
      title="수동주문입력"
      hint="다음 단계에서 거래처 select + 제품 행 + 저장 버튼 구현 예정"
    />
  );
}
